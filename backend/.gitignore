# 1. ملفات البيئة والمفاتيح السرية (أهم جزء أمنياً 🚨)
.env
.env.local
.env.production
.env.development

# 2. فولدر الموديلات والاعتمادات المحملة (مساحتها عملاقة وما تترفعش 📦)
node_modules/
jspm_packages/

# 3. فولدر ناتج عمل الـ Build أو الـ Compile لملفات التايب سكريبت (تتحول لـ JS)
dist/
build/
out/

# 4. السجلات والملفات المؤقتة للأخطاء (Logs)
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# 5. ملفات نظام التشغيل المؤقتة (عشان ما تبوظش شكل المستودع)
.DS_Store
Thumbs.db